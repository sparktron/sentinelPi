# SentinelPi — Development/Test Dependencies

# Testing
pytest>=7.4.0,<9.0
pytest-cov>=4.1.0,<6.0

# Linting / type checking
ruff>=0.15.0,<1.0
mypy>=1.11,<2.0
# Third-party type stubs for the libraries we import (scapy/waitress/maxminddb
# are untyped and ignored via [tool.mypy] overrides in pyproject.toml).
types-requests
types-PyYAML
types-psutil
types-python-dateutil
